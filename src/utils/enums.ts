/* eslint-disable no-unused-vars */

// TODO: Comprendre pourquoi je dois bypass ces 2 regles

// eslint-disable-next-line no-shadow
enum PRStatus {
  Ready = "ready",
  Rejected = "rejected",
  Pending = "pending",
  Draft = "draft",
}
export default PRStatus;
